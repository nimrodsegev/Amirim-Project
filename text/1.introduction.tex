\section{Introduction}
\label{sec:intro}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/motivation}
\caption{Why anticipation would be worth having. Emitting \textit{The Lord of
the Rings} one token at a time costs five full passes through a 32-layer model.
If the remainder were recoverable from the state at \textit{Lord}, most of that
work would be redundant. The lower row is hypothetical: the exit layer is
illustrative, and recoverability alone would not make such a shortcut safe or
cheap. Whether the remainder is recoverable at all is this paper's question; we
do not implement a decoding scheme.}
\label{fig:motivation}
\end{figure}

A decoder-only language model emits one token per forward pass, and pays the
full depth of the network for each one. This is not obviously necessary. Some
continuations are all but fixed by the time the model has read a few tokens:
after \textit{the Empire}, in a sentence about New York, very little other than
\textit{State Building} can follow. If the model's hidden state at
\textit{Empire} already encodes \textit{State Building}, then the two forward
passes that produce those tokens are re-deriving something the model had
already computed (Figure~\ref{fig:motivation}).

Recent interpretability work has shown that language models do build
representations of units larger than a single token. \citet{kaplan-etal-2025-tokens}
demonstrate an \emph{intrinsic detokenization} process: when a word is split
into several subword tokens, information from the earlier pieces is aggregated
into the last one, and the resulting state behaves like a representation of the
whole word, even for words the tokenizer never emits as a unit. That result,
however, concerns a unit the model has \emph{already finished reading}: it
establishes that the model consolidates evidence backwards, not that it
anticipates forwards. Our question is the forward one, and it is the one with
consequences for how models are run:

\begin{quote}
Given a partial phrase, does the model's internal representation already carry
enough information to recover the rest, before it has read or generated any of
it?
\end{quote}

A general form of this question is already answered in the affirmative.
\citet{pal-etal-2023-future} show that tokens two or more positions ahead can be
recovered from a single hidden state, by transplanting it into a fresh context
and reading what the model produces. What their setup cannot easily ask is
whether the anticipated material is \emph{a unit}: their reference is
whatever the model itself would have emitted next, so there is no target
independent of the model's own behaviour to hold fixed across occasions, or to
check the model's behaviour against.

We therefore study \emph{fixed multi-word expressions}: idioms, landmark
names, and film titles. For these the continuation is defined by the phrase
rather than by the model, which makes three things possible: a lookahead
distance that counts something meaningful, a repetition structure that lets us
ask whether anticipation is a property of the phrase or of the occasion, and a
comparison between what a transplanted state yields and what the model actually
writes. Their frequency in pretraining text also makes them the most favourable
case; if anticipation does not happen here, it is unlikely to happen anywhere.
We index the question by \emph{lookahead distance} $i$: the number of phrase
tokens still unread at the point where we interrupt the model. At $i=4$ in
\textit{The Lord of the Rings}, the model has read only \textit{The}, and we ask
whether \textit{Lord of the Rings} is recoverable from its state.

Recoverable by what means, however, turns out to matter as much as the answer.
We use two readouts on the same interrupted states. The first is
Patchscopes \citep{ghandeharioun-etal-2024-patchscopes}: we lift the hidden
state at the cut position and inject it into an unrelated prompt, and ask
whether the model verbalises the remaining tokens. The second simply lets the
model continue generating from the true prefix it has actually seen. The first
asks what a vector contains; the second asks what the model does. A successful
Patchscopes readout is routinely taken as evidence that the model ``knows''
something; we ask how far that licence extends.

Our findings are as follows. Anticipation at the phrase level is substantial:
in natural context, OLMo-2-7B produces the final two tokens of a familiar phrase
in $84.0\%$ of instances and the final five in $53.6\%$, against $59.2\%$ and
$17.6\%$ for the same phrases in isolation (\S\ref{sec:lookahead}). Recovery clusters strongly by phrase:
outcomes across a phrase's contexts are far more extreme than an
independent-context model at a common rate predicts
(\S\ref{sec:stability}). And it is decodable. A small classifier reading the
raw hidden state predicts whether the model will in fact complete the phrase, at
$76.8\%$ balanced accuracy and $87.6\%$ precision (\S\ref{sec:probe}).

The two readouts, however, do not measure the same thing. Run on identical
instances they agree on only $78.7\%$ of them, with generation recovering
continuations Patchscopes misses about three times as often as the reverse; they
reverse the ranking of the phrase types, idioms being easiest under Patchscopes
and hardest under generation; and the two probes trained on their respective
labels place the ``best'' layer in different parts of the network
(\S\ref{sec:disagreement}). Some of the asymmetry is an artifact of how many
layers the Patchscopes union is taken over, and we quantify how much; the
disagreement that remains is not, and survives the widest sweep we can run. We
take this as a caution rather than a refutation: Patchscopes remains informative
about representational content, but a claim of the form \emph{the model knows
$X$ at layer $\ell$} is not readout-independent, and should say which instrument
produced it, and over how many layers.
